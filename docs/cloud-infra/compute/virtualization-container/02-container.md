---
title: 리눅스 컨테이너 기초와 작동 원리
date: 2026-03-19
tags:
  - Virtualization & Container
---

## 들어가며
컨테이너는 가상머신(VM)과 달리 하드웨어를 가상화하거나 별도의 Guest OS를 구동하지 않습니다. 대신 호스트 OS의 커널을 공유하며 프로세스를 논리적으로 격리하여 실행합니다.

최근 많은 기업은 마이크로서비스 워크로드를 **애플리케이션 컨테이너**로 운영합니다. 애플리케이션 컨테이너는 서비스 실행에 필요한 바이너리, 라이브러리, 런타임 의존성을 이미지로 패키징해 배포 일관성과 이식성을 높입니다.
> "내 컴퓨터에서는 됐는데 왜 서버에선 안 돼?"와 같은 상황을 해결합니다.

반면 LXC/LXD와 같은 시스템 컨테이너는 전체 Linux 사용자 공간(user space)을 포함해 VM과 유사한 운영 경험을 제공할 수 있습니다. 다만 여전히 VM과 달리 호스트 커널을 공유한다는 점에서 격리 모델은 다릅니다.

시스템 컨테이너인지 애플리케이션 컨테이너인지에 관계없이, 이러한 컨테이너는 리눅스 커널 기능을 조합해 구현됩니다. 이 글에서는 격리와 자원 통제 관점에서 핵심이 되는 chroot/pivot_root, cgroups, Namespaces를 중심으로 핵심 리눅스 커널 기술을 정리해보겠습니다.

## 1. Linux chroot / pivot_root (Root 디렉토리 변경)
`chroot`는 프로세스가 인식하는 최상위 루트 디렉토리(`/`)를 특정 디렉토리로 변경하는 시스템 콜입니다. 

컨테이너 내부에 접근하면 호스트 OS의 전체 파일 시스템이 아닌 하위 집합만 표시됩니다. 이는 컨테이너 프로세스가 실행될 때 chroot 시스템콜을 통해 프로세스를 감옥(chroot jail)에 가두기 때문입니다. 감옥에 갇힌 프로세스는 자신에게 할당된 디렉토리 밖의 파일 시스템을 볼 수도, 접근할 수도 없게 됩니다.

하지만 `chroot`는 단독으로 사용 시 파일 시스템 레벨의 격리만 제공할 뿐, PID나 네트워크 인터페이스, 사용자 권한 같은 리소스 격리를 제공하지 않습니다.
현대 컨테이너 런타임에서는 mount namespace와 pivot_root, bind mounts를 조합하여 컨테이너 전용 rootfs를 구성하는 방식이 주로 사용됩니다.

`pivot_root`는 프로세스의 루트 파일 시스템을 다른 마운트로 전환할 때 사용됩니다. 컨테이너 런타임은 보통 mount namespace와 함께 `pivot_root`를 사용해 컨테이너 전용 rootfs를 구성하며, `chroot` 단독보다 더 안정적인 격리 기반을 만듭니다.

## 2. Linux cgroups (Control Groups) (자원 제어)
cgroup은 프로세스가 사용할 수 있는 메모리, CPU, 블록 I/O 등의 자원 사용량을 할당 및 제한하는 역할을 합니다. Linux에서 컨테이너는 프로세스로서 실행되므로, cgroups를 사용하여 각 컨테이너에 사용되는 리소스를 제한할 수 있습니다. 

Linux에서 직접 cgroup을 생성하고 관리할 수도 있지만, 컨테이너를 사용하는 경우 컨테이너 런타임이 이를 대신 처리합니다. 

> 쿠버네티스 Pod 스펙의 `resources.limits`는 런타임에서 cgroup 제한으로 직접 연결됩니다. `resources.requests`는 스케줄러의 배치 판단 기준이며, 노드 자원 예약/가중치 정책과 함께 동작합니다.

## 3. Linux Namespaces (가시성 격리)
Linux Namespaces는 프로세스별로 독립된 시스템 환경에 있는 것처럼 가시성을 격리해주는 리눅스 커널 기능입니다. 

> 쿠버네티스 환경에서 Pod 내부 구조를 이해하거나 네트워크를 트러블슈팅할 때 반드시 알아야 하는 핵심 개념입니다.

최초의 네임스페이스는 2002년 버전 2.4.19 Linux 커널에 마운트 네임스페이스로 도입되었으며, 현재 Linux에서는 여러 종류의 네임스페이스가 지원되고 있습니다:

- **Process ID(pid)**: 프로세스 ID를 격리합니다. 컨테이너 내부의 최초 실행 프로세스는 PID 1을 할당받습니다. 호스트에서 12345번으로 도는 프로세스라도, 컨테이너 내부에서는 자신을 1번(init) 프로세스라고 인식하게 됩니다. 
- **Network(net)**: 네트워크 인터페이스, IP 주소, 라우팅 테이블, 포트 번호 등을 격리합니다. 각 컨테이너가 자신만의 독립적인 네트워크 인터페이스와 IP를 가질 수 있는 이유입니다.
- **Mount Point(mnt)**: 파일 시스템 마운트 포인트를 격리합니다. 컨테이너 안에서 USB를 마운트해도 호스트나 다른 컨테이너에는 보이지 않습니다.
- **Inter-Process Communication(ipc)**: 프로세스 간 통신(Shared Memory, Semaphore, Message Queue 등)을 격리하여, 다른 컨테이너의 프로세스 메모리에 접근하는 것을 막습니다.
- **Unix Time Sharing System(uts)**: 호스트네임(Hostname)과 도메인 네임을 격리합니다. 실행 중인 호스트 이름과 독립적으로 컨테이너의 호스트네임(Hostname)과 도메인 네임을 변경할 수 있습니다. 
- **User(user)**: 사용자 및 그룹 ID를 격리합니다. 컨테이너 내부의 `root` 사용자는 호스트에서는 권한이 없는 일반 사용자 계정에 매핑하여 권한 상승 공격을 방지합니다.
  
<!-- 
## 4. UnionFS (OverlayFS) (파일 시스템 레이어링)
UnionFS(실무에서는 주로 OverlayFS)는 여러 디렉토리 레이어를 하나의 파일 시스템처럼 보이게 만드는 기술입니다. 최신 컨테이너 엔진은 보통 `overlay2` 스토리지 드라이버를 사용합니다.
컨테이너 이미지는 "겹겹이 쌓인 읽기 전용 파일 묶음"이고, 컨테이너 실행 시 맨 위에 "얇은 쓰기 공간"이 하나 추가됩니다.

- **이미지 레이어 (Read-Only)**: 컨테이너 이미지는 단계별 읽기 전용 레이어로 구성됩니다. 동일한 베이스 이미지를 여러 컨테이너가 사용할 때 레이어를 공유하므로 디스크 사용량을 크게 줄일 수 있습니다.
- **컨테이너 레이어 (Writable)**: 컨테이너가 시작되면 최상단에 쓰기 가능한 레이어가 붙습니다. 로그 파일 생성, 임시 파일 작성, 설정 파일 변경 같은 런타임 변경은 이 레이어에 기록됩니다.
- **Copy-on-Write (CoW)**: 읽기 전용 레이어의 파일을 수정해야 할 때, 해당 파일을 쓰기 레이어로 복사한 뒤 변경합니다. 원본 레이어는 유지되므로 이미지 불변성이 보장되고 배포 재현성이 높아집니다.

동작 흐름을 한 번에 보면 다음과 같습니다.

1. 이미지 pull: 필요한 레이어만 내려받아 로컬 캐시에 저장합니다.
2. 컨테이너 run: 기존 읽기 전용 레이어 위에 쓰기 레이어를 1개 추가합니다.
3. 파일 변경 발생: 변경 대상 파일만 CoW 방식으로 복사 후 수정합니다.
4. 컨테이너 종료/삭제: 쓰기 레이어는 사라지고, 읽기 전용 이미지 레이어는 재사용됩니다.

실무에서 중요한 포인트는 두 가지입니다.

- **성능/운영 관점**: 컨테이너 내부에 데이터베이스 데이터나 대용량 로그를 오래 쌓으면 쓰기 레이어가 커져 성능과 관리성이 나빠질 수 있습니다. 지속 데이터는 볼륨(Volume)으로 분리하는 것이 일반적입니다.
- **개념 구분**: OverlayFS는 "격리 기술"이라기보다 "이미지/스토리지 효율 기술"입니다. 격리는 Namespaces와 cgroups가 담당하고, OverlayFS는 빠른 배포와 이미지 재사용 효율을 담당합니다. -->

## 요약
컨테이너는 가상머신처럼 별도 OS를 띄우지 않으면서 리눅스 커널의 격리/제어 기능을 결합해 애플리케이션 실행 환경을 독립적으로 구성하는 방식입니다.

리눅스 컨테이너는 아래 3가지 기술을 중심으로 동작합니다:

- `chroot/pivot_root`: 프로세스가 바라보는 최상위 루트 파일 시스템 범위를 분리합니다.
- `Namespaces`: 프로세스별 시스템 리소스의 가시성을 격리합니다.
- `cgroups`: CPU, 메모리, I/O 같은 물리 자원 사용량을 제한하고 통제합니다.
<!-- 
 이 원리를 이해하면 Kubernetes Pod 구조, 리소스 제한(`requests`/`limits`), 운영 중 성능 문제를 훨씬 명확하게 해석할 수 있습니다.

### 실무 체크리스트 (3가지)
- 컨테이너가 예상보다 많은 자원을 쓰면 먼저 `limits` 설정과 cgroup 적용 상태를 확인합니다.
- Pod 내부 통신 이슈가 생기면 Network namespace, 서비스 포트, DNS 순서로 점검합니다.
- 파일 경로/권한 문제가 반복되면 `chroot/pivot_root` 관점과 mount namespace, 볼륨 마운트 구성을 함께 확인합니다. -->